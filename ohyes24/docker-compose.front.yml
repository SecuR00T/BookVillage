services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: yes24-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-19081}:80"
    networks:
      - yes24-net

networks:
  yes24-net:
    name: yes24-net
