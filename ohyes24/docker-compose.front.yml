services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: bookchon-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-19081}:80"
    networks:
      - bookchon-net

networks:
  bookchon-net:
    name: bookchon-net

