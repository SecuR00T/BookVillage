services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: yes24-frontend
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-8081}:80"
    networks:
      - yes24-net

networks:
  yes24-net:
    external: true
    name: yes24-net
